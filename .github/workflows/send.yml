name: send
on:
  repository_dispatch:
    types: [send_pack]
  schedule:
    - cron: '30 7 * * *'
jobs:
  send:
    runs-on: ubuntu-latest
    env:
      TOKEN: "8672154032:AAEgSGwR7uXGZsdpxNJvgsXswJ4aHoAYzhE"
      GH_PAT: ${{ secrets.GH_PAT }}
      OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}
      CUSTOM: ${{ toJSON(github.event.client_payload) }}
    steps:
      - name: deps
        run: |
          sudo apt-get update -y > /dev/null 2>&1
          sudo apt-get install -y ffmpeg > /dev/null 2>&1
          pip install pillow > /dev/null 2>&1
      - name: run studio
        run: |
          sleep 5
          curl -s "https://mbms-1356.github.io/forexin-site-/studio.py?v=$(date +%s)" -o studio.py
          python3 studio.py
